---
type: index-doc
title: "04-导读路径"
doc_type: walkthrough
tags:
 - sglang/index-layer
 - sglang/batch/30
 - sglang/doc/walkthrough
updated: 2026-07-02
---

# 04 · 导读路径

> 基于 KG `tour[]`（15 步）展开：每步叙事 + 嵌入代码 + 与下一站的衔接

本导读假设你**不打开 sglang 源码树**，仅读本目录文档即可建立心智模型。按 order 顺序走，约 4–6 小时可完成首轮。

---

## Step 0 · 零基础先修

**目标：** 建立 LLM serving 基础直觉——Prefill/Decode、TTFT/TPOT、KV Cache、三进程模型、前缀缓存。

**适用读者：** **无 LLM serving 经验必读。** 若你从未部署过推理服务、不清楚 prefill 与 decode 的区别、或看到 TTFT/KV Cache 等词就发懵，请先读本篇再进入 Step 1。已有 vLLM/TRT-LLM 经验者可跳过，直接看 [[08-设计追问与框架对比|08-设计追问与框架对比]] 中的框架差异。

**Explain：** 本篇用餐厅类比 + 极简 mermaid，把后续 15 步 tour 反复出现的概念一次讲清。读完后应能口头回答：「一条 HTTP 请求如何变成流式 token」「为什么需要 KV Cache」「SGLang 为什么拆三个进程」。

**Code：**

```markdown
<!-- 阅读入口 -->
07-总结与索引/00-零基础先修.md
```

**Comment：**

- 约 30 分钟；采用 ETC（Explain/Code/Comment）格式，与全库一致。
- 术语速查见 [[术语表#零基础速查生活类比版|术语表 · 零基础速查]]。
- 完成 Step 0 后，按顺序：**00-方法论 → 01-项目总览 → 全链路请求追踪 → 本导读 Step 1–8**。

**→ 下一站：** 项目定位与 monorepo 结构

---

## Step 1 · 项目总览

**目标：** 从 README 理解 SGLang 定位——高性能 LLM/VLM 服务框架，核心卖点 RadixAttention 与连续批处理。

SGLang 不是「又一个 HuggingFace generate 封装」。它的设计目标是：**在单卡到千卡集群上，把 prefill 吞吐、decode 延迟、前缀复用率同时优化**。README 中的 News 条目（DeepSeek-V4 day-0、GB300 25x、DFlash Spec V2）都指向同一方向——系统级推理优化。

**Explain：** KG 边 `document:README.md → concept:radix-attention` 表明官方文档把 RadixAttention 作为首要特性介绍。

**Code：**

```markdown
<!-- 来源：README.md 定位段 -->
SGLang is a fast serving framework for large language models and vision language models.
```

**Comment：** 读完本篇后，打开 [[01-项目总览]] 看 monorepo 结构与启动链。

**→ 下一站：** 弄清代码物理分布

---

## Step 2 · 仓库与包结构

**目标：** 理解 monorepo 顶层与 `python/sglang` 子包分工：`srt`=运行时，`lang`=前端 DSL，`multimodal_gen`=扩散。

Python 包是用户 `pip install` 的直接产物；`srt/` 占代码量 80% 以上。`sgl-kernel/` 与 `rust/sglang-grpc/` 编译为扩展，普通 Python 读源码时容易忽略。

**Explain：** `python/sglang/README.md` 列出子目录职责，KG 边将其链接到 `module:srt` 与 `module:lang`。

**Code：**

```toml
# 来源：python/pyproject.toml
[project.scripts]
sglang = "sglang.cli.main:main"
```

**Comment：** 一切用户可见命令从 `sglang.cli.main` 开始。

**→ 下一站：** 命令行如何进入运行时

---

## Step 3 · CLI 入口

**目标：** 掌握推荐命令 `sglang serve` 的分发逻辑：`cli/main → cli/serve → launch_server`。

**Explain：** `main()` 用 argparse subparsers 注册 serve/generate/version；serve 子命令全部委托给 `cli/serve.py`。

**Code：**

```python
# 来源：python/sglang/cli/main.py L37-L40
    if args.subcommand == "serve":
        from sglang.cli.serve import serve

        serve(args, extra_argv)
```

**Comment：** `extra_argv` 是 `--model-path` 等 server 参数，未经 main 层解析。

**→ 下一站：** launch_server 四路分发

---

## Step 4 · 服务启动分发

**目标：** 理解 `run_server` 在 HTTP（默认）、gRPC、Ray、Encoder-only 间的选择。

绝大多数开发者只走 HTTP 分支。gRPC 分支走 legacy SMG path；Ray 需额外安装 `sglang[ray]`。

**Explain：** 判断顺序：encoder_only → grpc_mode → use_ray → default HTTP。

**Code：**

```python
# 来源：python/sglang/launch_server.py L47-L51
    else:
        # Default mode: HTTP mode.
        from sglang.srt.entrypoints.http_server import launch_server

        launch_server(server_args)
```

**Comment：** HTTP 分支会 spawn Scheduler + Detokenizer 子进程。

**→ 下一站：** 可选 gRPC 深潜，或跳过到 Step 6

---

## Step 5 · gRPC 与 Proto（可选）

**目标：** 从 `sglang.proto` 到 Rust Tonic + Python RuntimeHandle，理解跨语言桥接。

若只做 HTTP 部署可跳过。若做 gateway 或多语言 client，此步必读。

**Explain：** 数据流：gRPC request → SglangServiceImpl → PyBridge → RuntimeHandle.generate_request → ChunkCallback 流式返回。

**Code：**

```python
# 来源：python/sglang/srt/entrypoints/grpc_bridge.py L262-L281
    def submit_request(
        self,
        *,
        req_type: str,
        req_dict: dict,
        chunk_callback,
        is_disconnected_fn: Optional[Callable[[], bool]] = None,
    ):
        mock_request = (
            _GrpcRequest(is_disconnected_fn=is_disconnected_fn)
            if is_disconnected_fn is not None
            else None
        )
        if req_type == "generate":
            from sglang.srt.managers.io_struct import GenerateReqInput

            obj = GenerateReqInput(**req_dict)
            stream = req_dict.get("stream", False)
            self._submit_on_tm_loop(
                self._run_generate(obj, chunk_callback, stream, mock_request)
```

**Comment：** 背压通过 mpsc channel 容量与 ChunkSendStatus 实现。

**→ 下一站：** 公共 API

---

## Step 6 · 公共 Python API

**目标：** 理解 `import sglang` 获得的 Frontend 与 Runtime API，以及 LazyImport 策略。

Frontend 适合离线编排；Runtime 适合 embedding Engine 或连接 Remote Endpoint。

**Explain：** `__init__.py` 顶部 macOS stub 保证 import 不依赖 triton。

**Code：**

```python
# 来源：python/sglang/__init__.py L81-L90
__all__ = [
    "Engine",
    "Runtime",
    "assistant",
    "assistant_begin",
    "assistant_end",
    "flush_cache",
    "function",
    "gen",
    "gen_int",
```

**Comment：** `Engine` 是 in-process 推理；`sglang serve` 是 out-of-process 服务。

**→ 下一站：** 请求出去后谁处理——Detokenizer

---

## Step 7 · Detokenizer 与输出反序列化

**目标：** 理解 `sglang::detokenizer` 子进程如何把 `BatchTokenIDOutput` 增量解码为 `BatchStrOutput`。

这是全链路的「回程第一站」。Scheduler 只产出 token id；用户看到的 text 全在 Detokenizer 完成。

**Explain：** event_loop 阻塞读 ZMQ → dispatch handler → 写回 TokenizerManager。

**Code：**

```python
# 来源：python/sglang/srt/managers/detokenizer_manager.py L161-L168
    def event_loop(self):
        """The event loop that handles requests"""
        while True:
            with self.soft_watchdog.disable():
                recv_obj = sock_recv(self.recv_from_scheduler)
            output = self._request_dispatcher(recv_obj)
            if output is not None:
                sock_send(self.send_to_tokenizer, output)
```

**Comment：** UTF-8 边界处理在 `_decode_batch_token_id_out` 内，见 concept:incremental-decoding。

**→ 下一站：** GPU 执行栈

---

## Step 8 · ModelRunner 与 TP Worker

**目标：** 掌握 `ScheduleBatch → ForwardBatch → ModelRunner.forward` 的 decode step 执行栈。

**Explain：** TpModelWorker 是 Scheduler 调用的唯一 execution 门面；内部持有 ModelRunner 实例。

**Code：**

```python
# 来源：python/sglang/srt/managers/tp_worker.py L506-L510
        if self.pp_group.is_last_rank:
            out = self.model_runner.forward(
                forward_batch,
                pp_proxy_tensors=pp_proxy_tensors,
            )
```

**Comment：** CUDA Graph 路径在 ModelRunner 内根据 batch 特征决定，见 batch 17 Attention 专题。

**→ 下一站：** 权重从哪来

---

## Step 9 · 模型加载与 Registry

**目标：** 理解 LoadFormat、DefaultModelLoader、ModelRegistry EntryClass 与 Llama/Qwen 通用实现。

**Explain：** `registry.py` 按 HF `config.architectures` 查找 `EntryClass`；`loader.py` 按 LoadFormat 选加载策略。

**Code：**

```python
# 来源：python/sglang/srt/models/registry.py L80-L91
    def resolve_model_cls(
        self,
        architectures: Union[str, List[str]],
    ) -> Tuple[Type[nn.Module], str]:
        architectures = self._normalize_archs(architectures)

        for arch in architectures:
            model_cls = self._try_load_model_cls(arch)
            if model_cls is not None:
                return (model_cls, arch)

        return self._raise_for_unsupported(architectures)
```

**Comment：**

- HF `config.architectures` 列表按序尝试，首个命中即返回 `(EntryClass, arch_name)`。
- `_try_load_model_cls` 查 `ModelRegistry.models` 字典，由启动时 `register("sglang.srt.models")` 扫描 `EntryClass` 填充。
- 加载失败走 `_raise_for_unsupported`，错误信息列出全部尝试过的 architecture。

**Comment：** 新模型支持通常只需加 `srt/models/xxx.py` 并注册 EntryClass。

**→ 下一站：** 专用模型与 Radix 缓存

---

## Step 10 · 专用模型与 Radix 前缀缓存

**目标：** DeepSeek MLA+MoE、Mllama Vision、Gemma3 sliding attention；RadixCache match/insert 流程。

**Explain：** 专用模型在通用 Llama 结构上叠加 MLA、cross-attention、alternating sliding window；RadixCache 与模型无关，Scheduler 统一调用。

**Code：**

```python
# 来源：python/sglang/srt/mem_cache/radix_cache.py L355-L359
    def match_prefix(self, params: MatchPrefixParams) -> MatchResult:
        """Find the longest cached prefix of ``key`` in the radix tree.

        The logical namespace for prefix matching is determined by both the
        token id sequence and the optional ``extra_key`` carried by ``RadixKey``.
```

**Comment：** UnifiedRadixCache 扩展支持 SWA/Mamba/HiCache，见 batch 16 KV Cache 专题。

**→ 下一站：** 吞吐优化——投机解码

---

## Step 11 · 投机解码

**目标：** SpeculativeAlgorithm → EAGLEWorkerV2：Draft/Verify 两阶段与 reject sampling。

**Explain：** Worker 替换发生在 Scheduler 初始化时；forward mode 在 draft extend 与 target verify 间交替。

**Code：**

```python
# 来源：python/sglang/srt/speculative/spec_info.py L28-L35
class SpeculativeAlgorithm(Enum):
    """Builtin speculative decoding algorithms. Plugin-registered ones are
    ``CustomSpecAlgo`` instances; ``from_string`` returns either type, and
    both expose the same ``is_*()`` / ``create_worker`` interface so callers
    dispatch uniformly without isinstance checks.
    """

    DFLASH = auto()
```

**Comment：** batch 21 专题有 EAGLE/DFlash/Spec V2 完整时序图。

**→ 下一站：** 集群扩展——PD 分离

---

## Step 12 · Prefill-Decode 分离

**目标：** PD 分离队列模型与 KV 跨节点传输：prefill.py / decode.py。

**Explain：** Prefill 节点 extend 完成后通过 KVSender 传 KV；Decode 节点 recv 后以 ForwardMode.PREBUILT 入队。

**Code：**

```python
# 来源：python/sglang/srt/disaggregation/prefill.py L104-L107
class PrefillBootstrapQueue:
    """
    Store the requests in bootstrapping
    """
```

**Comment：** transfer_backend 可选 Mooncake、NIXL 等，见 batch 22 专题。

**→ 下一站：** 多卡并行

---

## Step 13 · 分布式并行

**目标：** `initialize_model_parallel`、GroupCoordinator collective 路由、DataParallelController fan-out。

**Explain：** TP/PP/EP/DP/CP 各自有 ProcessGroup；GroupCoordinator 统一 all_reduce/all_gather 入口。

**Code：**

```python
# 来源：python/sglang/srt/distributed/parallel_state.py L1967-L1979
def initialize_model_parallel(
    tensor_model_parallel_size: int = 1,
    expert_model_parallel_size: int = 1,
    pipeline_model_parallel_size: int = 1,
    attention_data_parallel_size: int = 1,
    attention_context_model_parallel_size: int = 1,
    moe_data_model_parallel_size: int = 1,
    decode_context_parallel_size: int = 1,
    backend: Optional[str] = None,
    duplicate_tp_group: bool = False,
    enable_symm_mem: bool = False,
    recovered_rank: bool = False,
) -> None:
```

**Comment：** DP Controller 用 ZMQ 把请求 fan-out 到多 Scheduler，策略有 ROUND_ROBIN 等。

**→ 下一站：** 多模态

---

## Step 14 · 多模态 VLM

**目标：** Processor 注册、`get_mm_processor` 与 BaseMultimodalProcessor 预处理链路。

**Explain：** 图像/视频/音频在 TokenizerManager 侧预处理为 embedding 或 token 序列，再送入 Scheduler。

**Code：**

```python
# 来源：python/sglang/srt/multimodal/processors/base_processor.py L402-L472
    def process_mm_data(
        self, input_text, images=None, videos=None, audios=None, **kwargs
    ) -> dict:
        """
        process multimodal data with transformers AutoProcessor
        """
        if images:
            kwargs["images"] = images
            if self.image_config:
                kwargs.setdefault("images_kwargs", {}).update(self.image_config)
        if videos:
            kwargs["videos"] = videos
            if self.video_config:
                kwargs.setdefault("videos_kwargs", {}).update(self.video_config)
        if audios:
            if self._processor.__class__.__name__ in {
                "Gemma3nProcessor",
                "Gemma4Processor",
                "Gemma4UnifiedProcessor",
                "GlmAsrProcessor",
                "Qwen2AudioProcessor",
                "Qwen3ASRProcessor",
                "Qwen3OmniMoeProcessor",
            }:
                # Note(Xinyuan): for gemma3n, ref: https://github.com/huggingface/transformers/blob/ccf2ca162e33f381e454cdb74bf4b41a51ab976d/src/transformers/models/gemma3n/processing_gemma3n.py#L107
                kwargs["audio"] = audios
                kwargs.setdefault("audio_kwargs", {})
                kwargs["audio_kwargs"].setdefault("truncation", False)
            else:
                kwargs["audios"] = audios
            if self.audio_config:
                kwargs.setdefault("audio_kwargs", {}).update(self.audio_config)

        processor = self._processor
        if (
            hasattr(processor, "image_processor")
            and isinstance(processor.image_processor, BaseImageProcessor)
            and not self.server_args.disable_fast_image_processor
        ):
            if _is_cpu or get_global_server_args().rl_on_policy_target is not None:
                kwargs["device"] = "cpu"
            elif _is_xpu:
                kwargs["device"] = "xpu"
            elif not _is_npu:
                base_gpu_id = get_global_server_args().base_gpu_id
                kwargs["device"] = f"cuda:{base_gpu_id}"
            elif processor.__class__.__name__ not in {
                "Glm4vProcessor",
                "Glm46VProcessor",
            }:
                # Note: for qwen-vl, processor has some reshape issue because of dims restriction on Ascend.
                from sglang.srt.hardware_backend.npu.modules.qwen_vl_processor import (
                    npu_apply_qwen_image_preprocess_patch,
                )

                npu_apply_qwen_image_preprocess_patch()
                kwargs["device"] = "npu"
            elif processor.__class__.__name__ == "Glm46VProcessor":
                from sglang.srt.hardware_backend.npu.modules.glm46v_processor import (
                    npu_apply_glm46v_image_preprocess_patch,
                )

                npu_apply_glm46v_image_preprocess_patch()
                kwargs["device"] = "npu"

        result = processor.__call__(
            text=[input_text],
            padding=True,
            return_tensors="pt",
            **kwargs,
        )
```

**Comment：**

- 图像/视频/音频 kwargs 分别注入 HF AutoProcessor；部分 ASR 模型用 `audio` 而非 `audios` 键。
- 返回 dict 含 `input_ids` 与 `pixel_values` 等，后续 `process_and_combine_mm_data` 展开 special token。
- `keep_mm_feature_on_device=False` 时 feature tensor 落 CPU，经 ZMQ IPC 传到 Scheduler GPU 侧。

**Comment：** batch 24 专题覆盖 Qwen-VL 等具体 Processor。

**→ 下一站：** LoRA 多租户

---

## Step 15 · LoRA 多适配器服务

**目标：** LoRAManager、MemoryPool 与 CSGMV batch 多租户 LoRA。

**Explain：** 请求携带 `lora_path`；Manager 确保 adapter 在 GPU MemoryPool 中，batch 内按 req 索引选权重。

**Code：**

```python
# 来源：python/sglang/srt/lora/lora_manager.py L57-L78
class LoRAManager:
    def __init__(
        self,
        base_model: torch.nn.Module,
        base_hf_config: AutoConfig,
        max_loras_per_batch: int,
        load_config: LoadConfig,
        dtype: torch.dtype,
        server_args: ServerArgs,
        lora_backend: str = "triton",
        tp_size: int = 1,
        tp_rank: int = 0,
        max_lora_rank: Optional[int] = None,
        target_modules: Optional[Iterable[str]] = None,
        lora_paths: Optional[List[LoRARef]] = None,
    ):
        self.base_model: torch.nn.Module = base_model
        if hasattr(base_hf_config, "get_text_config"):
            self.base_hf_config: AutoConfig = base_hf_config.get_text_config()
        else:
            self.base_hf_config: AutoConfig = base_hf_config
        self.max_loras_per_batch: int = max_loras_per_batch
```

**Comment：** batch 25 专题有 CSGMV kernel 与 eviction 细节。

---

## Step 16 · 用户故事导读

**目标：** 用「第一个请求从进入到响应」的用户视角，把 Step 1–15 的模块名翻译成可感知的行为——TTFT、流式 token、abort、多租户 LoRA 各对应哪一跳。

**Explain：** 技术导读按组件切分；用户故事按时间线切分。读完 [[07-用户故事与场景]] 后，再回看 [[全链路请求追踪]] 会更快建立因果链。

**Code：**

```python
# 来源：python/sglang/srt/entrypoints/http_server.py L790-L791
async def generate_request(obj: GenerateReqInput, request: Request):
    """Handle a generate request."""
```

**Comment：** 用户故事文档将覆盖：冷启动 warmup、流式 SSE、客户端断开 abort、OpenAI 兼容路径与 Native API 的差异。与 Step 4（HTTP 启动）、Step 7（Detokenizer 回程）形成闭环。

**→ 下一站：** [[07-用户故事与场景]] → 全链路串讲

---

## 导读收官 · 全链路串讲

完成 16 步后，阅读 [[全链路请求追踪]] 把 HTTP 请求从进入到响应的 7 个 hop 串成一条线；若走 gRPC 部署，对照 [[全链路请求追踪-gRPC]]。再用 [[05-文件地图]] 作查阅索引，[[06-复杂度热点]] 标记需要深读的文件。常见问题见 [[04-关键问题]]。

```mermaid
flowchart LR
 S1[Step1 总览] --> S3[Step3 CLI]
 S3 --> S4[Step4 启动]
 S4 --> S7[Step7 Detokenizer]
 S7 --> S8[Step8 ModelRunner]
 S8 --> S10[Step10 RadixCache]
 S10 --> S11[Step11 投机]
 S11 --> S12[Step12 PD]
 S12 --> S15[Step15 LoRA]
 S15 --> S16[Step16 用户故事]
 S16 --> E2E[全链路追踪]
```

---

## 附录：KG tour 26 步完整索引表

Knowledge Graph `tour[]` 共 **26 步**；上文正文为时间受限读者的 **15 步精简导读**（右列「精简 Step」）。深潜某步时，跳转「对应专题目录」阅读专题文档。

| Order | KG Title | 对应专题目录 | 精简 Step |
|------:|----------|--------------|-----------|
| 1 | 项目总览 | [[00-方法论-00-MOC|`00-方法论`]] · [[01-项目总览]] | Step 1 |
| 2 | 仓库与包结构 | [[02-启动链路-00-MOC|`02-启动链路`]] | Step 2 |
| 3 | CLI 入口 | [[02-启动链路-00-MOC|`02-启动链路`]] | Step 3 |
| 4 | 服务启动分发 | [[03-HTTP-Server-00-MOC|`03-HTTP-Server`]] | Step 4 |
| 5 | Engine 与子进程架构 | [[03-HTTP-Server-00-MOC|`03-HTTP-Server`]] | Step 4 / 6 |
| 6 | HTTP 服务与 API 挂载 | [[03-HTTP-Server-00-MOC|`03-HTTP-Server`]] | Step 4 |
| 7 | OpenAI 兼容 API | [[04-OpenAI-API-00-MOC|`04-OpenAI-API`]] | — |
| 8 | Ollama 兼容 API | [[04-OpenAI-API-00-MOC|`04-OpenAI-API`]] | — |
| 9 | gRPC 与 Proto | [[05-gRPC-Proto-00-MOC|`05-gRPC-Proto`]] | Step 5 |
| 10 | TokenizerManager 请求入口 | [[06-TokenizerManager-00-MOC|`06-TokenizerManager`]] | Step 6 下游 |
| 11 | Scheduler 与调度策略 | [[07-Scheduler-00-MOC|`07-Scheduler`]] · [[08-SchedulePolicy-00-MOC|`08-SchedulePolicy`]] | Step 7 上游 |
| 12 | ScheduleBatch 数据结构 | [[09-ScheduleBatch-IO-00-MOC|`09-ScheduleBatch-IO`]] | Step 8 上游 |
| 13 | Detokenizer 输出反序列化 | [[10-Detokenizer-00-MOC|`10-Detokenizer`]] | Step 7 |
| 14 | ModelRunner 与 TP Worker | [[11-ModelRunner-00-MOC|`11-ModelRunner`]] | Step 8 |
| 15 | 模型加载与 Registry | [[12-ModelLoader-00-MOC|`12-ModelLoader`]] · [[13-Models-通用-00-MOC|`13-Models-通用`]] | Step 9 |
| 16 | Llama/Qwen/DeepSeek 模型实现 | [[14-Models-专用-00-MOC|`14-Models-专用`]] | Step 10 |
| 17 | Radix 前缀 KV 缓存 | [[15-RadixAttention-00-MOC|`15-RadixAttention`]] · [[16-KV-Cache-00-MOC|`16-KV-Cache`]] | Step 10 |
| 18 | Attention Backend 与 Kernel | [[17-Attention-00-MOC|`17-Attention`]] · [[26-sgl-kernel-00-MOC|`26-sgl-kernel`]] | — |
| 19 | MoE 与 FP8 量化 | [[18-MoE-00-MOC|`18-MoE`]] · [[19-Quantization-00-MOC|`19-Quantization`]] | — |
| 20 | 采样参数 | [[20-Sampling-00-MOC|`20-Sampling`]] | — |
| 21 | 投机解码 | [[21-Speculative-00-MOC|`21-Speculative`]] | Step 11 |
| 22 | Prefill-Decode 分离 | [[22-Disaggregation-00-MOC|`22-Disaggregation`]] | Step 12 |
| 23 | 分布式并行 | [[23-Distributed-00-MOC|`23-Distributed`]] | Step 13 |
| 24 | 多模态 VLM | [[24-Multimodal-00-MOC|`24-Multimodal`]] | Step 14 |
| 25 | LoRA 多适配器服务 | [[25-LoRA-00-MOC|`25-LoRA`]] | Step 15 |
| 26 | 扩展收官：Gateway/Lang/Diffusion | [[27-model-gateway-00-MOC|`27-model-gateway`]] · [[28-Frontend-lang-00-MOC|`28-Frontend-lang`]] · [[29-multimodal_gen-00-MOC|`29-multimodal_gen`]] | — |

**Comment：**

- 「—」表示 KG 专步未纳入 15 步精简导读，按需跳转专题目录。
- 精简 Step 5（gRPC）在正文中标为可选；Step 6（公共 API）对应 KG 5/10 的交叉区域。
- tour nodeIds 详见 `sglang/.understand-anything/knowledge-graph.json` `tour[]`。

---

## 设计追问

### Q1：15 步精简导读省略了哪些 KG tour 节点？跳过的代价是什么？

**Explain：** KG 共 26 步，精简版合并了 OpenAI API、Attention Backend、MoE/Quant、Gateway 等。若你的部署只做 HTTP chat，Step 7–8 后应补读 OpenAI API（OpenAI）与 17（Attention）；若做 PD 集群，Step 12 后必须深读 PD 分离 而非仅看导读摘要。

**Code：**

```markdown
<!-- 附录表 Order 18–26 标记为「—」的条目即精简导读未覆盖项 -->
```

**Comment：** 用 [[05-文件地图]] 按文件名反查专题，比死记 26 步顺序更高效。

---

### Q2：Step 16 用户故事与技术 Step 的阅读顺序可否对调？

**Explain：** 完全新手可先 Step 16 + 用户故事文档建立端到端图像，再回头读 Step 3–8 填模块细节。已有 vLLM/TRT-LLM 经验的读者建议维持 Step 1→16 顺序，避免用户故事里的行为描述缺少 Scheduler/Radix 术语支撑。

**Code：**

```python
# 来源：python/sglang/srt/entrypoints/http_server.py L798-L800
                async for out in _global_state.tokenizer_manager.generate_request(
                    obj, request
                ):
```

**Comment：** 两种路径都应在收官读 [[全链路请求追踪]] 做 hop 级核对。

---

### Q3：可选 Step 5（gRPC）在什么条件下从「可跳过」变为「必读」？

**Explain：** 当部署架构引入 `sgl-model-gateway`、Rust gRPC server、或多语言 client 不走 HTTP 时，Step 5 的 `RuntimeHandle.submit_request` 与背压 callback 成为主路径。仅 Python FastAPI 部署可跳过，但需知 gRPC 与 HTTP 在 TokenizerManager 层汇合，调试技巧可迁移。

**Code：**

```python
# 来源：python/sglang/srt/entrypoints/grpc_bridge.py L55-L61
class RuntimeHandle:
    """Thin Python handle that the Rust gRPC server calls into.

    Provides synchronous ``submit_*``, ``abort``, and info methods.
    Each submit method receives a ``chunk_callback`` (a Rust-side PyO3 object)
    that it invokes with ``(chunk_dict, finished, error)`` for each response
    chunk produced by TokenizerManager.
```

**Comment：** [[全链路请求追踪-gRPC]] 是 Step 5 的收官练习。
