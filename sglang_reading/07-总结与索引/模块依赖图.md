---
type: index-doc
title: "模块依赖图"
doc_type: dataflow
tags:
 - sglang/index-layer
 - sglang/batch/30
 - sglang/doc/dataflow
updated: 2026-07-02
---

# 模块依赖图

> Mermaid 拓扑 + 关键 import/call 依赖关系

---

## 启动链依赖（CLI → SRT）

```mermaid
flowchart TD
 TOML["config:pyproject.toml"] -->|configures 0.6| CLI_MAIN["cli/main.py"]
 CLI_MAIN -->|calls 0.8| CLI_SERVE["cli/serve.py"]
 CLI_SERVE -->|calls 0.8| LAUNCH["launch_server.py"]
 LAUNCH -->|depends_on 0.7| SRT["module:srt"]
 LAUNCH -->|calls 0.85| GRPC_SRV["grpc_server.py"]
 LAUNCH -->|calls| HTTP_SRV["http_server.py"]
 HTTP_SRV -->|contains| TM["TokenizerManager"]
 HTTP_SRV -->|spawns| SCH["Scheduler"]
 HTTP_SRV -->|spawns| DET["DetokenizerManager"]
```

**说明：** 实线箭头表示调用或依赖关系；括号内权重表示关系强弱，仅作阅读优先级参考。HTTP 是默认路径，gRPC 为可选分支。

---

## 运行时核心依赖

```mermaid
flowchart LR
 SRT["module:srt"] -->|depends_on 0.6| KERNEL["sgl-kernel"]
 SRT -->|contains 0.9| MR["model_runner.py"]
 SRT -->|contains| SCH["scheduler.py"]
 SCH -->|calls| TPW["tp_worker.py"]
 TPW -->|calls 0.9| MR
 MR -->|calls 0.85| LOADER["model_loader/loader.py"]
 MR -->|uses| RADIX_ATTN["layers/radix_attention.py"]
 RADIX_ATTN -->|depends_on| RADIX_CACHE["mem_cache/radix_cache.py"]
 SCH -->|ZMQ| DET["detokenizer_manager.py"]
 TM["tokenizer_manager.py"] -->|ZMQ| SCH
 DET -->|ZMQ| TM
```

---

## gRPC 跨语言依赖

```mermaid
flowchart TB
 PROTO["sglang.proto"] -->|depends_on 0.95| BUILD_RS["build.rs"]
 BUILD_RS --> GRPC_MOD["module:sglang-grpc"]
 GRPC_MOD --> SERVER_RS["server.rs"]
 GRPC_MOD --> BRIDGE_RS["bridge.rs"]
 SERVER_RS -->|contains 0.95| SVC_IMPL["SglangServiceImpl"]
 BRIDGE_RS -->|contains 0.95| PY_BRIDGE["PyBridge"]
 BRIDGE_RS -->|calls 0.9| GRPC_PY["grpc_bridge.py"]
 GRPC_PY -->|contains 0.95| RT_HANDLE["RuntimeHandle"]
 RT_HANDLE -->|imports 0.85| IO_STRUCT["io_struct.py"]
 RT_HANDLE -->|calls| TM["TokenizerManager.generate_request"]
 GRPC_SRV["grpc_server.py"] -->|depends_on 0.9| SMG["smg-grpc-servicer"]
 SMG -->|calls 0.85| GRPC_MOD
```

**Explain：** Rust 侧不直接 import Python 模块；PyO3 在 runtime 加载 `RuntimeHandle` 实例。Proto 是 gateway 与 server 的共享契约。

---

## 高级特性模块依赖

```mermaid
flowchart TD
 SRT["module:srt"] -->|contains 0.85| DISAGG["module:disaggregation"]
 SRT -->|contains| SPEC["module:speculative"]
 SRT -->|contains| DIST["module:distributed"]
 SRT -->|contains| MM["module:multimodal"]
 SRT -->|contains| LORA["module:lora"]
 SPEC -->|implements| SPEC_CONCEPT["concept:speculative-decoding"]
 DISAGG -->|implements| PD_CONCEPT["concept:pd-disaggregation"]
 LORA -->|implements| LORA_CONCEPT["concept:lora-serving"]
 DISAGG -.->|related 0.6| SPEC
 RADIX["concept:radix-attention"] -->|depends_on 0.7| ALLOC["mem-cache-allocator"]
```

---

## 公共 API 依赖

```mermaid
flowchart LR
 INIT["__init__.py"] -->|imports 0.7| LANG["module:lang"]
 INIT -->|depends_on 0.6| SRT["module:srt"]
 INIT -->|imports 0.7| GLOBAL["global_config.py"]
 README["README.md"] -->|documents 0.5| RADIX_C["concept:radix-attention"]
 README -->|documents 0.5| CB["concept:continuous-batching"]
 README -->|documents 0.5| PD["concept:pd-disaggregation"]
```

---

## 关键依赖表

| Source | Target | Type | Weight | 含义 |
|--------|--------|------|--------|------|
| cli/main.py | cli/serve.py | calls | 0.8 | CLI 子命令分发 |
| cli/serve.py | launch_server.py | calls | 0.8 | LLM 启动 |
| launch_server.py | grpc_server.py | calls | 0.85 | gRPC 模式 |
| detokenizer_manager.py | incremental-decoding | implements | 0.9 | 增量解码实现 |
| forward_batch_info.py | forward-mode | implements | 0.9 | ForwardMode 定义 |
| radix-attention | radix_cache.py | implements | 0.9 | 前缀 cache 后端 |
| speculative-decoding | module:speculative | implements | 0.9 | 投机模块 |
| pd-disaggregation | module:disaggregation | implements | 0.9 | PD 模块 |
| lora-serving | module:lora | implements | 0.9 | LoRA 模块 |
| ModelRunner | model_loader/loader.py | calls | 0.85 | 权重加载 |
| bridge.rs | grpc_bridge.py | calls | 0.9 | PyO3 桥 |
| grpc-py-bridge | PyBridge | related | 0.9 | 概念↔类 |
| grpc-py-bridge | RuntimeHandle | related | 0.9 | 概念↔类 |

---

## 读图建议

1. **纵向追请求：** 从 `http_server → tokenizer_manager → scheduler → tp_worker → model_runner` 沿 calls 边向下
2. **横向追特性：** 从 `concept:*` 节点沿 implements 边找到模块，再沿 contains 找文件
3. **跨语言：** gRPC 子图独立阅读，注意 Rust→Python 边方向是 runtime 动态调用而非静态 import

完整节点列表见 [[05-文件地图]]；概念释义见 [[术语表]]。
